# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# ── 用户数据（不提交，每个运行实例独立）──
data/jobs/*
!data/jobs/.gitkeep
data/jobs.json
data/custom_providers.json
data/model_routes.json
data/library.json
data/library/
data/*.log
data/*.bak
data/uvicorn*.log
outputs/*
!outputs/.gitkeep

# 本地 AI 引擎与模型权重（下载后不进入 Git，通常为数 GB）
engines/*
!engines/.gitkeep

# 自动下载的 FFmpeg（tools/ffmpeg/bin）
tools/

# ── 生成的临时文件 ──
*.mp4
*.wav
*.jpg
*.png
*.srt
*.pcm
*.part
*.crdownload
*.tmp

# ── 测试/临时产物 ──
work/frames/
work/__pycache__/

# ── 环境与密钥 ──
.env
*.env
.env.*
!.env.example

# ── 打包产物 ──
dist-release/
dist-windows/
dist-electron/
node_modules/

# ── IDE / 编辑器 ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS 文件 ──
.DS_Store
Thumbs.db

# ── 内部材料与本地产物（公开发布隔离）──
work/
*-product-notes.md
.hf-cache/
.review-ledger.json
.review-diff.patch
*.log
*.bak*
.pytest_cache/

# ── 本机私有目录（含密钥或用户素材，发布前务必排除）──
_backup-*/
runtime-jobs/
logs/
qwen-controls/

# 本地服务握手 token 与端口记录（每次启动重新生成）
agent_token.txt
current_url.txt
